{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compilar Java",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-Command",
        "Remove-Item -Recurse -Force bin; mkdir bin; javac -d bin -sourcepath src (Get-ChildItem -Recurse -Filter *.java -Path src | ForEach-Object { $_.FullName })"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [
        "$javac"
      ]
    }
  ]
}
